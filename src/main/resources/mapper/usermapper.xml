<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board.repository.V1.mapper.UserMapper">

    <insert id="save" parameterType="board.entity.V1.Member">
        insert into member
            (
             username,
             password,
             name,
             email,
             role
            )
            values (#{username}, #{password}, #{name}, #{email}, #{role})

    </insert>

    <select id="findByUsername" parameterType="java.lang.String" resultType="board.entity.V1.Member">
        select id,
               username,
               password,
               name,
               email,
               role,
               created_at,
               updated_at
        from member
        where username = #{username}
    </select>

    <select id="existsByUsername" resultType="java.lang.Boolean">
        select case when count(*) = 1 then 1 else 0 end
        from member
        where username = #{username}
    </select>

    <select id="existsByEmail" resultType="java.lang.Boolean">
        select case when count(*) = 1 then 1 else 0 end
        from member
        where email = #{email}
    </select>
</mapper>